import $ from "jquery";
import React from "react";
import * as ReactDOM from 'react-dom/client';

const MyApp = () => {
    return <p>Hello World !!</p>
};

// Uncomment these lines and extension will append this react component to the document.body.

// const root = ReactDOM.createRoot(document.body);
// let comp = <MyApp />;
// root.render(comp);